app.get('/cart', (req, res) => {
    const cartItems = [
        // An array of objects representing cart items with properties like name, description, price, imageUrl, quantity, etc.
    ];

    // Calculate cart total dynamically
    const cartTotal = cartItems.reduce((total, item) => total + item.price * item.quantity, 0);

    res.render('cart', { cartItems, cartTotal });
});